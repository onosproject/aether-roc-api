# Helmit tests for aether-roc-umbrella

To run with [Helmit] use:
```bash
helmit test ./cmd/aether-roc-tests -c ../sdran-helm-charts --suite roc -n test
```

[Helmit]: https://docs.onosproject.org/helmit/docs/cli/
